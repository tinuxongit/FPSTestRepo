import * as THREE from 'three';
export function createSky(scene,biome){scene.background=new THREE.Color(biome.sky);scene.fog=new THREE.FogExp2(biome.fog,.009);const stars=new THREE.BufferGeometry(),pts=[];if(biome.label.includes('Orbital')){for(let i=0;i<900;i++){const r=220,th=Math.random()*Math.PI*2,ph=Math.acos(2*Math.random()-1);pts.push(r*Math.sin(ph)*Math.cos(th),r*Math.cos(ph),r*Math.sin(ph)*Math.sin(th));}stars.setAttribute('position',new THREE.Float32BufferAttribute(pts,3));const p=new THREE.Points(stars,new THREE.PointsMaterial({color:0xffffff,size:.35}));scene.add(p);return p;}return null;}
