import * as THREE from 'three';
function noisyTexture(base,accent,size=128){const c=document.createElement('canvas');c.width=c.height=size;const x=c.getContext('2d');x.fillStyle=base;x.fillRect(0,0,size,size);for(let i=0;i<2800;i++){const v=Math.random()*.12;x.fillStyle=`rgba(${accent},${v})`;x.fillRect(Math.random()*size,Math.random()*size,1+Math.random()*3,1+Math.random()*3);}const t=new THREE.CanvasTexture(c);t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(3,3);return t;}
export function createMaterials(biome){const ground=noisyTexture('#252a31','255,255,255'),concrete=noisyTexture('#555d65','255,255,255');return{ground:new THREE.MeshStandardMaterial({color:biome.ground,map:ground,roughness:.92,metalness:.02}),concrete:new THREE.MeshStandardMaterial({map:concrete,color:0x6a737b,roughness:.82,metalness:.08}),metal:new THREE.MeshStandardMaterial({color:0x242c35,roughness:.35,metalness:.82}),dark:new THREE.MeshStandardMaterial({color:0x12171d,roughness:.58,metalness:.55}),accent:new THREE.MeshStandardMaterial({color:biome.accent,emissive:biome.accent,emissiveIntensity:2.5})};}
