import { mulberry32 } from '../core/math.js';
export function generateLayout(seed,biome){const r=mulberry32(seed);const colliders=[],structures=[],spawn=[];const size=72;const border=36;colliders.push({minX:-border,maxX:border,minZ:-border,maxZ:-border+.8},{minX:-border,maxX:border,minZ:border-.8,maxZ:border},{minX:-border,maxX:-border+.8,minZ:-border,maxZ:border},{minX:border-.8,maxX:border,minZ:-border,maxZ:border});for(let i=0;i<30;i++){let x=(r()-.5)*58,z=(r()-.5)*58;if(Math.hypot(x,z)<8){i--;continue;}const w=2.5+r()*7,d=2.5+r()*7,h=1.5+r()*10;structures.push({x,z,w,d,h,variant:Math.floor(r()*4)});colliders.push({minX:x-w/2,maxX:x+w/2,minZ:z-d/2,maxZ:z+d/2});}for(let i=0;i<40;i++){const x=(r()-.5)*62,z=(r()-.5)*62;if(!colliders.some(c=>x>c.minX-1&&x<c.maxX+1&&z>c.minZ-1&&z<c.maxZ+1))spawn.push({x,z});}return{size,colliders,structures,spawn,seed,biome};}
