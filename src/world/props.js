import * as THREE from 'three';
export function addBiomeProps(scene,biome,rng,count=60){const group=new THREE.Group();for(let i=0;i<count;i++){const x=(rng()-.5)*68,z=(rng()-.5)*68;if(Math.hypot(x,z)<7)continue;let m;if(biome.label.includes('Jungle')||biome.label.includes('Marsh')){m=new THREE.Mesh(new THREE.CylinderGeometry(.1,.25,2+rng()*4,6),new THREE.MeshStandardMaterial({color:0x31452e}));}else if(biome.label.includes('Glacial')){m=new THREE.Mesh(new THREE.ConeGeometry(.3+rng(),1+rng()*3,5),new THREE.MeshStandardMaterial({color:0xc8e0e8,roughness:.4}));}else{m=new THREE.Mesh(new THREE.BoxGeometry(.15+rng()*.5,.3+rng()*2,.15+rng()*.5),new THREE.MeshStandardMaterial({color:0x3b4248,metalness:.45,roughness:.6}));}m.position.set(x,m.geometry.parameters.height?m.geometry.parameters.height/2:.5,z);m.rotation.y=rng()*Math.PI;group.add(m);}scene.add(group);return group;}
