import { OPERATIONS } from '../data/maps.js';import { CLASS_LIST,CLASSES } from '../data/classes.js';
export class Menu {constructor({profile}){this.profile=profile;this.selectedOp=OPERATIONS[0].id;this.selectedClass=profile.lastClass||'vanguard';this.deployFn=null;this.build();}build(){const sel=document.getElementById('operation-select');for(const o of OPERATIONS){const opt=document.createElement('option');opt.value=o.id;opt.textContent=`${String(o.index).padStart(2,'0')} · ${o.name}`;sel.append(opt);}sel.onchange=()=>{this.selectedOp=sel.value;this.updateOp();};const picker=document.getElementById('class-picker');for(const c of CLASS_LIST){const b=document.createElement('button');b.textContent=c.name;b.dataset.id=c.id;b.onclick=()=>{this.selectedClass=c.id;this.updateClass();};picker.append(b);}this.updateOp();this.updateClass();document.getElementById('deploy-button').onclick=()=>this.deployFn?.({operationId:this.selectedOp,classId:this.selectedClass,endless:false});document.getElementById('endless-button').onclick=()=>this.deployFn?.({operationId:'endless',classId:this.selectedClass,endless:true});}updateOp(){const o=OPERATIONS.find(x=>x.id===this.selectedOp);document.getElementById('operation-description').textContent=o.description;}updateClass(){const c=CLASSES[this.selectedClass];document.getElementById('class-description').textContent=`${c.tagline}. ${c.ability}: ${c.abilityDescription}`;document.querySelectorAll('#class-picker button').forEach(b=>b.classList.toggle('selected',b.dataset.id===c.id));}onDeploy(fn){this.deployFn=fn;}show(){document.getElementById('menu').classList.add('show');}}
