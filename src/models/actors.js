import * as THREE from 'three';
const armorMat = c => new THREE.MeshStandardMaterial({color:c,roughness:.42,metalness:.62});
function limb(g,w,h,d,x,y,z,mat){const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d,2,2,2),mat);m.position.set(x,y,z);m.castShadow=true;g.add(m);return m;}
export function createSoldier(){const g=new THREE.Group(),dark=armorMat(0x161c23),armor=armorMat(0x283440),glow=new THREE.MeshStandardMaterial({color:0xff252d,emissive:0xff1018,emissiveIntensity:3});limb(g,.7,.85,.42,0,1.35,0,armor);limb(g,.78,.24,.48,0,1.55,-.02,dark);const head=new THREE.Mesh(new THREE.SphereGeometry(.26,12,8),dark);head.position.y=2.02;head.castShadow=true;head.userData.head=true;g.add(head);const visor=limb(g,.34,.08,.06,0,2.02,-.245,glow);visor.userData.head=true;for(const s of[-.2,.2])limb(g,.22,.82,.25,s,.52,0,dark);for(const s of[-.46,.46]){const a=limb(g,.17,.78,.2,s,1.32,0,armor);a.rotation.z=s<0?-.08:.08;}limb(g,.52,.18,.7,.22,1.25,-.43,dark);return g;}
export function createBrute(){const g=createSoldier();g.scale.set(1.55,1.55,1.55);const core=new THREE.Mesh(new THREE.OctahedronGeometry(.18),new THREE.MeshBasicMaterial({color:0xff4b38}));core.position.set(0,1.47,-.36);g.add(core);return g;}
export function createDrone(){const g=new THREE.Group(),m=armorMat(0x202932);const body=new THREE.Mesh(new THREE.SphereGeometry(.42,12,8),m);body.position.y=1.6;body.castShadow=true;g.add(body);for(let i=0;i<4;i++){const a=i*Math.PI/2;const arm=limb(g,.65,.09,.14,Math.cos(a)*.52,1.6,Math.sin(a)*.52,m);arm.rotation.y=-a;}const eye=new THREE.Mesh(new THREE.SphereGeometry(.1,8,6),new THREE.MeshBasicMaterial({color:0xff2932}));eye.position.set(0,1.6,-.4);eye.userData.head=true;g.add(eye);return g;}
